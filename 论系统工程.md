---
layout: article
title: "论系统工程"
permalink: "/论系统工程.html"
seo_title: "论系统工程：从需求、模型与接口到验证和持续演化｜邹德虎 Dehu Zou"
description: "以电力系统和实时仿真为例，讨论系统工程如何连接需求、边界、模型、接口、权衡、验证确认与组织协同，并辨析软件定义、数字孪生和人工智能的适用边界。"
updated: "2026-09-07"
---

# 论系统工程

<div class="article-meta" style="margin:-4px 0 24px;color:#667085;font-size:.9rem;display:flex;gap:8px 18px;flex-wrap:wrap;"><span>作者：邹德虎</span><span>修订：2026-09-07</span></div>

在电力系统和工业软件研发中，我们经常遇到一种现象：单个设备经过了试验，算法推导没有明显错误，各个软件模块也能独立运行，但系统集成以后，仍然出现性能不足、控制冲突、数据不一致或难以维护的问题。

问题未必出在某个专业做得不够好，而可能出在专业之间：需求理解不同，模型边界不同，接口语义不同，或者每个局部都在优化自己的指标，却没有人对整体结果负责。

这正是我理解系统工程的起点。**系统工程不是把问题说得更宏大，也不是由少数人画完架构图，再把实现交给别人；它是让需求、技术、组织和证据围绕同一个目标形成闭环的方法。** INCOSE 的定义强调跨学科与综合集成，ISO/IEC/IEEE 15288 则从系统生命周期建立过程框架。[1][2] 对工程人员而言，重要的不是背诵定义，而是把这些思想转化为可执行、可检查的工作。

## 1. 先分清：系统思维、控制理论和系统工程

系统思维提醒我们关注整体、关系、反馈和环境。控制理论为动态系统的分析与调节提供数学工具。系统工程则要把这些认识落实到一个实际系统的需求定义、方案选择、设计实现、集成、运行维护乃至退役中。三者互有联系，却不能相互替代。

例如，一次调频可以作为控制问题研究；一次调频功能接入真实电网，则还涉及频率测量、死区与限幅、机组可用裕度、执行机构、通信、测试和运行责任。仅有一条稳定性证明，并不等于这项工程已经完成；同样，仅有完善的项目流程，也不能弥补控制机理上的错误。

跨学科类比很有启发，但必须说明边界。状态估计包含量测校验，却不等同于一般的数据清洗；它还依赖网络模型、可观测性和统计假设。调频死区与通信切换中的滞回、触发持续时间，都有抑制频繁动作的考虑，但不是同一种数学机制。PWM 是一种调制方式，必须结合功率电路、滤波与控制才能实现具体功能，不能把它直接等同于整流、逆变或主动降噪。

我的看法是：跨学科能力首先表现为能够辨认共同结构，其次才是借用方法。只看到相似的词语而忽略不同的前提，反而容易造成误解。

## 2. 需求：把“做得好”变成可以检验的问题

“高性能”“高可靠”“智能化”可以表达愿望，却还不是完整的工程要求。需求应说明系统服务谁、在什么场景中工作、受到哪些约束，以及怎样判断交付结果是否合格。NASA 的系统工程手册把运行概念、利益相关方期望和可验证需求放在设计工作的前端。[3]

以电力系统实时仿真平台为例，“能够实时运行”至少需要继续展开：模型规模与元件类型是什么？采用什么时间步长？是否包含控制器和外部输入输出？允许什么误差？拓扑变化时是否仍满足计算期限？出现超时或通信异常后，系统应采取什么动作？

这些问题不能由一个“每秒计算多少次”的平均值代替。假设某任务必须在每个仿真步结束前完成，平均耗时小于步长，并不能排除偶发超时。验收需要明确测试负载、运行条件、计时范围和超时判据；测试观察到的最大耗时，也不能在没有进一步论证时冒充所有可能工况的严格最坏时间界。

同样，“提高可靠性”要说明关注的是故障概率、连续运行时间、失效后的安全状态，还是服务恢复时间。这些指标相关，却不相同。

因此，需求不是越多越好，也不是越细越好。真正有价值的需求，应当必要、清晰、相互一致，而且能够追溯到业务目标和验证方法。把某种实现方案过早写成唯一需求，可能反而排除更合适的设计。

## 3. 边界与模型：详细并不自动等于正确

工程分析总要选择边界。做常规正序潮流时，我们用节点功率和电压约束描述稳态；研究机电动态时，需要加入转子、调速和励磁等状态；研究开关暂态、波过程或特定变流器相互作用时，则可能需要电磁暂态模型。

这些模型不是简单的“低级—高级”关系。模型的适用性取决于研究对象、频段、时间尺度、参数质量和验证证据。详细模型若使用了未经确认的参数，也可能给出貌似精确、实际不可靠的结果。

我倾向于在模型旁边同时写下四件事：保留了什么，忽略了什么，参数从哪里来，结果准备拿来做什么决策。这样，模型才不容易在传递过程中被当成无条件成立的事实。

分层分解也有同样的要求。按专业、时间尺度或空间区域划分系统，可以降低局部工作的复杂度，但必须保留对目标问题重要的耦合。把一个控制器单独调得很好，不代表它与其他控制器连接后仍然稳定；把网络拆成多个并行子系统，也不代表接口延迟不会改变原系统的动态。

复杂系统的难点，往往不只是元件数量，而是**局部行为经过耦合以后，产生了局部试验看不到的整体行为**。因此，模块分解与系统集成必须同时考虑，不能等所有模块做完以后才讨论接口。

## 4. 接口：不仅要传得过去，还要理解一致

很多工程问题表面上是“通信问题”，实际上是接口语义没有定义完整。双方都成功收到了一个数值，却未必在描述同一个物理量。

电力系统软件的接口，至少要明确单位和标幺基准、功率与电流的正方向、相序、参考坐标、时间戳、采样与保持方式，以及设备标识和拓扑版本。数据质量标志、失效值、超时、重传和重启后的行为，也属于接口契约，而不是临时补丁。

例如，同一个无功功率数值，一方以发出为正，另一方以吸收为正，接口在语法上完全正确，控制方向却可能相反。又如，拓扑已经更新，量测还停留在上一时刻，即使每个数据项单独看来有效，组合后的运行断面也可能没有物理意义。

因此，接口管理不是把字段写进文档就结束了。它还需要版本管理、兼容策略、自动化检查和跨专业的联调证据。接口变更应当触发影响分析：哪些模型、程序、测试和运行规程需要一起修改？

## 5. 权衡：局部最优为什么可能伤害整体

系统设计通常同时面对精度、实时性、可靠性、可维护性、成本和研制周期。它们之间存在约束，不能脱离场景宣布某种技术全面优越。

例如，实时仿真采用更小步长，可能减小某些离散误差，却增加计算和通信压力；引入更多并行资源，可能缩短局部计算时间，却增加同步开销和故障模式；增加冗余节点，可能提高某类故障下的可用性，却也可能引入共享电源、共用网络、同源软件缺陷等共因失效。

所谓冗余，不是把部件数量加倍就自然得到可靠系统。还要分析故障独立性、故障检测覆盖、切换条件、切换期间的行为，以及是否存在所有副本共同依赖的薄弱环节。电网的 N−1 校核也应在明确的故障集、运行方式和判据下理解，不能把它等同于对任意事故的绝对保证。

我的一个基本判断是：先写出约束，再比较方案；先明确不能牺牲的底线，再讨论效率提升。对于安全相关系统，不能拿正常工况下的平均收益，掩盖少见但后果严重的失效路径。

## 6. 验证与确认：既要做对，也要做对的系统

系统工程中的 Verification 与 Validation 经常一起出现，但两者回答不同的问题：验证关注实现是否满足规定要求；确认关注系统在预定使用场景中，是否满足实际需要。[4]

例如，一个求解器通过解析算例、残差检查和回归测试，是验证的重要组成部分。但用户要用它评价某类变流器的控制相互作用，还必须确认所选模型、参数、时间尺度和误差水平能够支持这项判断。不同软件结果接近是一种证据，却不能单独证明它们都正确，因为它们可能共享同一个不恰当的假设。

验证活动也不应只在项目末期进行。需求阶段应考虑验收方法；架构阶段应验证关键技术风险；实现阶段应进行单元、接口和集成测试；交付前应覆盖典型场景、边界场景和故障场景；运行后则应把问题记录转化为新的回归用例。

对仿真平台，我尤其看重一条可追溯链：

> 研究问题 → 模型假设 → 参数与数据 → 实现版本 → 测试场景 → 误差与性能结果 → 可以支持的结论。

链条中任何一环改变，都可能需要重新评价后面的结论。一次现场运行成功很有价值，但不应被扩大为对全部场景的保证；一次试验失败也应回到证据和复现条件，而不是先归因于某个人的能力。

## 7. 软件定义、数字孪生与 AI：机会来自边界清晰

软件使控制逻辑、业务规则和交互方式更容易修改，是工业系统持续演化的重要工具。但软件复制成本低，不等于工业软件的全生命周期成本低。测试、部署、停机窗口、兼容性、安全审查和长期维护，都可能成为主要成本。

“软件定义”也没有消除硬件。数字式保护仍依赖模拟前端、采样、供电、开入开出和跳闸回路；软件定义网络强调控制与转发的解耦，并不要求全部控制功能只能运行在一台服务器上。更准确的设计目标，是把变化较快的策略与相对稳定的机制合理分离，同时维护两者之间的约束。

数字化建模有助于把问题发现时机前移，但仿真结果不能代替全部实物验证。数字孪生的价值也不在于三维画面是否逼真，而在于它能否把真实对象的数据、模型和决策持续联系起来。基于模型的系统工程可以帮助维护需求、结构、行为和验证之间的关系；画图本身并不等于建立了这套关系。NASA 的系统建模手册提供了相应工作产品与建模组织方式的实例。[5]

AI 可以辅助检索资料、整理需求、生成候选方案、编写代码或发现异常，但输出仍要进入工程验证流程。尤其在电力生产控制中，错误建议、分布外输入、数据泄露和不可追溯的模型变化，不能只靠“人工看一下”笼统处理。我更赞成根据后果分级确定权限、独立校核和回退机制，而不是把 AI 的建议直接等同于可执行的控制指令。NIST 的 AI 风险管理框架可作为风险识别和持续评价的参考，而不是替代行业规程的通行证。[6]

## 8. 组织协同：总体责任不能只停留在图纸上

原文讨论过钱学森的总体设计思想。对我而言，其中值得带入今天研发工作的，是跨专业统筹、总体技术责任，以及理论、试验和工程经验的结合；不必通过未经核对的名人引语来证明这些工作的重要性。

总体技术负责人不一定亲自编写全部代码，也不可能掌握所有专业细节，但必须理解关键实现约束，并有能力追问和审查证据。反过来，专业工程师也需要知道自己的模块为什么存在、影响哪些用户和系统指标。架构与实现之间应当双向反馈，而不是只有单向任务下达。

系统工程与项目管理相互配合，但侧重点不同：进度、预算和资源安排不能替代技术一致性，技术方案优秀也不代表组织协同和交付计划自然成立。管理反馈不宜只统计“完成了多少任务”，还要关注消除了什么风险、形成了什么证据、留下了哪些假设。

敏捷迭代与系统工程并不矛盾。不可轻易修改的硬件和安全约束，需要较早澄清；可逆的软件试验，可以缩短反馈周期。关键是让变更可追溯、影响可分析、验证能跟上，而不是用“敏捷”省略必要的设计，也不是用“总体设计”阻止来自实现和用户的反馈。

## 9. 结语：把复杂性变成可以承担的责任

回到开头的问题：为什么部件都能工作，系统却仍然可能失败？因为系统的正确性，不是部件正确性的简单相加。需求、边界、接口、时序、异常行为和组织责任，都参与决定最终结果。

我理解的系统工程，可以用六个问题来检验：我们究竟要解决什么问题？模型和系统的边界在哪里？局部之间怎样相互影响？方案取舍的依据是什么？有什么证据支持它能够工作？环境变化以后，由谁负责重新评价？

**系统工程的价值，不在于把所有细节集中到一个人手中，而在于让复杂系统中的关键关系、关键假设和关键责任，不再处于无人说明、无人验证、无人维护的状态。**

## 参考资料与修订说明

[1] INCOSE. [About Systems Engineering](https://www.incose.org/about-systems-engineering/).

[2] ISO/IEC/IEEE 15288:2023. [Systems and software engineering — System life cycle processes](https://www.iso.org/standard/81702.html). 此处引用公开的标准范围说明，不据此声称已核阅付费标准的全部条款。

[3] NASA. [Systems Engineering Handbook, 4.0 System Design Processes](https://www.nasa.gov/reference/4-0-system-design-processes/).

[4] NASA. [Systems Engineering Handbook, Appendix](https://www.nasa.gov/reference/system-engineering-handbook-appendix/)，需求验证矩阵及验证与确认计划相关部分。

[5] NASA. [NASA-HDBK-1009, Systems Modeling Handbook for Systems Engineering](https://standards.nasa.gov/standard/NASA/NASA-HDBK-1009)，官方范围与工作产品说明。

[6] NIST. [AI Risk Management Framework](https://www.nist.gov/itl/ai-risk-management-framework).

本次修订重组了原文结构，删除了缺乏可靠出处的历史细节、长篇转引和可识别的负面人物点评；保留并重新展开跨学科、总体责任、软件演化和工程实践的主旨。文中的实时仿真示例用于解释方法，不构成某一产品的性能承诺或电网操作规程。
